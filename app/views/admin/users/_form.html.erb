<%= form_for [:admin, user] do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
    <%= f.label :email, required: true, autofocus: true %>
    <%= f.text_field :email %>

    <%= f.label :password, required: true if !user.nil? %>
    <%= f.text_field :password if !user.nil? %>

  </div>

  <div class="actions">
    <%= f.submit "Create User",  class: "btn-large green white-text center" %>
  </div>
<% end %>
